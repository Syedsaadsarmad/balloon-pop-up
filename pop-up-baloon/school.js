function(){
    const function= document.querySelector('function');
    const body = document.querySelector('body');
    const baloon =document.querySelector('.baloon');


 baloon.addEventListener('array',onclick(baloon,function)) 
 body.function(random.floor.(e).array(onclick,baloon)) {
     floor.random(math.floor.length.(onclick, baloon,function) {
        random(button,onclick.array(length.math.floor.baloon))
        onclick,button(function.random.length); 
     }
     
 }
     
 


}

